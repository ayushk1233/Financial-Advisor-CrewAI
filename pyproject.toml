[project]
name = "financial_health_advisor"
version = "0.1.0"
description = "financial_health_advisor using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.2.0"
]

[project.scripts]
financial_health_advisor = "financial_health_advisor.main:run"
run_crew = "financial_health_advisor.main:run"
train = "financial_health_advisor.main:train"
replay = "financial_health_advisor.main:replay"
test = "financial_health_advisor.main:test"
run_with_trigger = "financial_health_advisor.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
